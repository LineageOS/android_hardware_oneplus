android_app {
    name: "OnePlusPocketMode",

    srcs: ["src/**/*.kt"],

    certificate: "platform",
    platform_apis: true,
    product_specific: true,

    optimize: {
        proguard_flags_files: ["proguard.flags"],
    },
}
